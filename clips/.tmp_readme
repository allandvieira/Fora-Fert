Clips tasks:

create every forage file.
set rules on each file, acessing facts and set:
    about message
    repair message[
        calage and etc message
        n repair message
        p repair message
        k repair message
    ]

change main file to:
    load_forage_rules
    print_(or something like this)_recommendation


